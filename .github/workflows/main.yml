name: HProfileActions
on: workflow_dispatch
jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        run: actions/checkout@v4

      - name: Run test
        run: mvn test

      - name: Checkstyle
        run: mvn checkstyle:checkstyle
    
